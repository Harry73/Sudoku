// Main class to run
public class Driver {
	public static void main(String[] args) {
		// Pass the command line argument to Solver, used to select debug mode
		if (args.length == 0) {
			Solver j = new Solver("false");
		}
	}
}